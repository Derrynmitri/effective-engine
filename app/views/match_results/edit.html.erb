<% content_for :title, "Editing match result" %>
<div class="w-full flex flex-col gap-6">
  <h1 class="font-semibold text-lg">Update a Match Result</h1>
  <div class="w-full max-w-2xl p-6 bg-white rounded-lg shadow-md dark:bg-gray-800">
    <%= render "form", match_result: @match_result %>
  </div>
</div>